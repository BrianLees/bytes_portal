<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :event_description %>
  </div>

  <div class="field">
    <%= form.label :location_id %>
    <%= form.text_field :location_id, id: :event_location_id %>
  </div>

  <div class="field">
    <%= form.label :event_time %>
    <%= form.datetime_select :event_time, id: :event_event_time %>
  </div>

  <div class="field">
    <%= form.label :is_approved %>
    <%= form.check_box :is_approved, id: :event_is_approved %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
